package <%= @package %>.persistence.repositories;

import <%= @package %>.persistence.entities.<%= @entity.name.split('_').collect(&:capitalize).join %>;
import org.springframework.data.jpa.repository.JpaRepository;

<% if @entity.primary_key.is_uuid? -%>import java.util.UUID;<% end -%>

public interface <%= @entity.name.split('_').collect(&:capitalize).join %>Repository extends JpaRepository<<%= @entity.name.split('_').collect(&:capitalize).join %>,<%= @entity.primary_key.type %>> {

    Long deleteBy<%= @entity.primary_key.name.split('_').collect(&:capitalize).join %>(<%= @entity.primary_key.type %> <%= @entity.primary_key.name %>);

}