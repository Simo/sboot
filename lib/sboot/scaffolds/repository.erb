package <%= @package %>.persistence.repositories;

import <%= @package %>.persistence.entities.<%= @name.split('_').collect(&:capitalize).join %>;
import org.springframework.data.jpa.repository.JpaRepository;

public interface <%= @name.split('_').collect(&:capitalize).join %>Repository extends JpaRepository<<%= @name.split('_').collect(&:capitalize).join %>,Long> {

Long deleteById(Long id);

}