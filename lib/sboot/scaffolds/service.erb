package <%= @package %>.business.services;

import <%= @package %>.business.dtos.<%= @name.split('_').collect(&:capitalize).join %>DTO;

import java.util.List;

public interface <%= @name.split('_').collect(&:capitalize).join %>Service {

List<<%= @name.split('_').collect(&:capitalize).join %>DTO> trovaTutti();

  <%= @name.split('_').collect(&:capitalize).join %>DTO trova(Long id);

  <%= @name.split('_').collect(&:capitalize).join %>DTO salva(<%= @name.split('_').collect(&:capitalize).join %>DTO model);

  Long cancella(Long id);
  }