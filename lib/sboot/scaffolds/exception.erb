package <%= @package %>.business.exceptions;

public class <%= @entity.name.capitalize %>EntityNotFoundException extends RuntimeException {
    private Long id;
    private String klass;

    public <%= @entity.name.capitalize %>EntityNotFoundException(String klass, Long id) {
        this.klass = klass;
        this.id = id;
    }

    public Long getId() {
        return id;
    }

    public String getKlass(){
        return klass;
    }

    @Override
    public String getMessage() {
        return "" + klass + " entity with ID '" + id + "' not found";
    }
}
