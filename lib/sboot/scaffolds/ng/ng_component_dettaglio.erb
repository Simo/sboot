import { Component, OnInit } from '@angular/core';
import { <%= @entity.single_capitalize  -%> } from "../<%= @entity.single_downcase  -%>";
import { <%= @entity.single_capitalize  -%>Service } from "../<%= @entity.single_downcase  -%>.service";

@Component({
selector: 'app-<%= @entity.single_downcase  -%>-dettaglio',
templateUrl: './<%= @entity.single_downcase  -%>-dettaglio.component.html',
styleUrls: ['./<%= @entity.single_downcase  -%>-dettaglio.component.css']
})
export class <%= @entity.single_capitalize  -%>DettaglioComponent implements OnInit {

  <%= @entity.single_downcase  -%> = new <%= @entity.single_capitalize  -%>();

  constructor(private service: <%= @entity.single_capitalize  -%>Service) { }

  ngOnInit() {
    this.service.<%= @entity.single_downcase  -%>.subscribe(<%= @entity.single_downcase  -%> => {
      this.<%= @entity.single_downcase  -%> = <%= @entity.single_downcase  -%>;
    });
  }

}
